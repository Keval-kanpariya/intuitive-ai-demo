apiVersion: v1
kind: PersistentVolume
metadata:
  name: gcp-hostpath-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /mnt/gcp-disk
